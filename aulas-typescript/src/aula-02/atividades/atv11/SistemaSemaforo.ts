/**
 * 9. Tipos Literais: 
 * Crie um tipo literal chamado TrafficLight com os valores "red", "yellow" e "green". 
 * Em seguida, crie uma variável chamada currentLight e atribua um valor válido.
 */

import { TrafficLight } from "./model/type/TrafficLight";

let currentLight: TrafficLight = "Green";
console.log(currentLight);
